const Logo = () => {
    return (
        <div className="flex items-center space-x-2">
            <div>
                {/* Presto */}
                <span className="text-4xl font-bold text-blue-600">Presto</span>

                {/* Config */}
                <span className="text-4xl font-bold text-teal-500">Config</span>
            </div>
        </div>
    );
};

export default Logo;